# DEVTinder

    # Components
    -Body
        -navbar
        -Route ="/"  => feed
        -Route = "/login" => Login page
        -Route = "/connection" => Connection Page
        -Route = "Profile => Profile